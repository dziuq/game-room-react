"use strict";const distance=(t,s)=>Math.abs(t-s);function distance2D(t,s){const n=distance(t.x,s.x),c=distance(t.y,s.y);return Math.sqrt(n**2+c**2)}exports.distance=distance,exports.distance2D=distance2D;
